{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/freya.exe",
            "args": [],
            "preLaunchTask": "build_debug",
            "env": {},
            "cwd": "${workspaceFolder}"
        }
    ]
}